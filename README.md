# 🌿 گیاهیار - برنامه مراقبت از گیاهان

برنامه اندروید برای مراقبت از گیاهان آپارتمانی، گل‌های خانگی و درختان میوه.
ساخته‌شده با **Kivy** (Python) — قابل اجرا روی **Android** و **Desktop**.

---

## 📱 امکانات

| بخش | توضیح |
|-----|-------|
| 🌿 **لیست گیاهان** | ۸۵ گیاه با اطلاعات کامل و عکس واقعی + افزودن گیاه دلخواه |
| 🔍 **جستجو** | جستجوی فوری در نام، نام علمی، دسته‌بندی |
| 📋 **جزئیات گیاه** | توضیحات، شرایط رشد، مشکلات رایج، روش درمان |
| ⏰ **تایمر مراقبت** | تا ۱۰۰ تایمر نامگذاری‌شده برای آبیاری، کود، هرس و... |
| 🩺 **مشاوره گیاه‌پزشکی** | چت با هوش مصنوعی + ارسال عکس (نیاز به راه‌اندازی پراکسی، پایین همین صفحه) |
| 🛒 **فروشگاه** | لینک به جست‌وجوی گلدان، گیاه، بذر، کود در دیجی‌کالا |
| 📢 **تبلیغات** | بنر تبلیغاتی ۱×۳ سانتی‌متر در پایین صفحه |

---

## 🚀 نصب و راه‌اندازی

### اجرای روی Desktop (تست سریع)
```bash
pip install kivy==2.3.0 pillow requests
cd plant_care_app
python main.py
```

### ساخت APK اندروید

بیلد کیوی برای اندروید نیاز به Android SDK/NDK و چند گیگابایت فضا دارد،
پس دو راه دارید:

#### روش ۱ (پیشنهادی): بیلد آنلاین با GitHub Actions — بدون نیاز به لینوکس

این پروژه از قبل فایل `.github/workflows/build.yml` را دارد که APK را
روی سرورهای گیت‌هاب می‌سازد:

1. یک ریپازیتوری جدید در گیت‌هاب بسازید و **محتوای همین پوشه** (`plant_care_app`)
   را در ریشه‌ی آن push کنید (نه خودِ پوشه را با یک لایه‌ی اضافه).
2. به تب **Actions** ریپو بروید؛ بیلد به‌صورت خودکار شروع می‌شود (یا از
   همان‌جا با «Run workflow» دستی اجرایش کنید).
3. حدود ۱۵ تا ۲۵ دقیقه صبر کنید (بار اول بیشتر طول می‌کشد چون SDK/NDK
   دانلود می‌شود).
4. روی آخرین اجرا کلیک کنید → پایین صفحه بخش **Artifacts** → دانلود
   `giyahyar-apk`.

#### روش ۲: بیلد محلی (اگر Linux/Mac/WSL دارید)
```bash
pip install buildozer cython
# Linux/Mac:
sudo apt-get install -y python3-pip build-essential git python3 python3-dev \
    ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev \
    libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev zlib1g-dev

cd plant_care_app
buildozer android debug
# فایل APK در پوشه bin/ ساخته می‌شود
```

### نصب روی گوشی
```bash
adb install bin/plantcare-*-arm64-v8a-debug.apk
# یا فایل APK را مستقیم روی گوشی کپی و نصب کنید
```

---

## ⚙️ راه‌اندازی مشاوره هوش مصنوعی (اختیاری)

این بخش برای کار کردن به یک کلید API از Anthropic نیاز دارد. **کلید را
هرگز مستقیم داخل کد قرار ندهید** — چون هر فایل APK قابل استخراج است و
کلید فاش می‌شود. راهنمای کامل راه‌اندازی امن (پراکسی رایگان Cloudflare،
حدود ۵ دقیقه) در **[`server/README.md`](server/README.md)** است.

اگر این بخش را راه‌اندازی نکنید، بقیه‌ی اپ کامل کار می‌کند؛ فقط صفحه
«دامپزشک هوش مصنوعی» پیام «نیاز به تنظیم» نشان می‌دهد.

---

## 📁 ساختار پروژه

```
plant_care_app/
├── main.py                    ← نقطه ورود برنامه
├── buildozer.spec             ← تنظیمات ساخت APK
├── requirements.txt           ← وابستگی‌ها
│
├── .github/workflows/
│   └── build.yml              ← بیلد خودکار APK روی گیت‌هاب
│
├── server/
│   ├── cloudflare-worker.js   ← پراکسی امن برای کلید API
│   └── README.md              ← راهنمای راه‌اندازی پراکسی
│
├── data/
│   └── plants_db.py           ← پایگاه داده ۸۵ گیاه
│
├── screens/
│   ├── home_screen.py         ← صفحه اصلی
│   ├── plant_list_screen.py   ← لیست گیاهان + فیلتر
│   ├── plant_detail_screen.py ← جزئیات گیاه
│   ├── tools_screen.py        ← ابزار مراقبت
│   ├── timer_screen.py        ← تایمرها (تا ۱۰۰ عدد)
│   ├── ai_vet_screen.py       ← مشاوره هوش مصنوعی
│   ├── shop_screen.py         ← فروشگاه
│   ├── add_plant_screen.py    ← افزودن گیاه جدید
│   └── search_screen.py       ← جستجو
│
├── components/
│   └── ui_components.py       ← کامپوننت‌های مشترک UI (کارت، نوار ناوبری، تصویر گیاه و...)
│
├── utils/
│   ├── helpers.py             ← توابع کمکی (تاریخ فارسی، اعتبارسنجی و...)
│   └── generate_images.py     ← اسکریپت کمکی ساخت تصویر placeholder
│
└── images/                    ← عکس‌های واقعی ۸۵ گیاه + آیکون و اسپلش
    ├── app_icon.png
    ├── splash.jpg
    ├── sansevieria.jpg
    ├── monstera.jpg
    └── ...
```

---

## 🌱 گیاهان موجود در پایگاه داده (۸۵ گیاه)

**دسته‌بندی‌ها:**
- 🌿 **آپارتمانی** (۴۶ گونه): سانسوریا، پتوس، مونسترا، فیلودندرون، کاج نورفولک و...
- 🌸 **گلدار** (۲۰ گونه): ارکیده، آنتوریوم، بنفشه آفریقایی، رز، پوینسیتا و...
- 🌱 **دارویی** (۷ گونه): آلوئه‌ورا، نعناع، ریحان، رزماری و...
- 🍎 **درخت میوه** (۱۲ گونه): نارنج، لیمو، انجیر، توت‌فرنگی و...

---

## 💡 نکات مهم

- **تایمرها** در فایل `timers_data.json` روی خودِ گوشی ذخیره می‌شوند
- **گیاهان اضافه‌شده** در فایل `custom_plants.json` روی خودِ گوشی ذخیره می‌شوند
- **مشاوره هوش مصنوعی** نیاز به اینترنت و راه‌اندازی پراکسی دارد (بالا را ببینید)
- **بنر تبلیغاتی** در پایین همه صفحات (۳۰dp ≈ ۱ سانتی‌متر)
- گیاهانی که عکس واقعی ندارند (مثل گیاهان سفارشی که خودتان اضافه می‌کنید)
  به‌جای عکس، یک آیکون رنگی بر اساس دسته‌بندی نشان می‌دهند

---

## 📞 پشتیبانی

برای گزارش اشکال یا پیشنهاد، issue باز کنید.

---
**نسخه:** 1.0.0 | **فریمورک:** Kivy 2.3.0 | **زبان:** Python 3
